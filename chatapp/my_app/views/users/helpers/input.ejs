<% 
    inputsHelper = (name, typeName, placeholder,  id, className, options = null) => {
        return `<input name="${name}" type="${typeName}" placeholder="${placeholder}" class ='${className} id ='${id}/>`
    }
%>